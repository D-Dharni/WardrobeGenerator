This project takes in photos of clothing objects and turns those clothing objects into a
lookbook with the top three outfits. It does this by sending the clothing item into an AI
pipeline to extract the details, turning that information into clothing objects, using a
compatibility scoring algorithm to compare the two objects, and uses graph traversal to
try and find the best possible outfits.